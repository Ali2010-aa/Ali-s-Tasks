// let words = ["alma", "armud", "banan", "çiyələk"];

// let newArr = words.reduce((max, curr) => (curr.length > max.length ? curr : max));
// console.log(newArr);

// const users = ["Ali" , "Veli" , "Zara" , "Kamran"];
// function check (){
//     return users.some((value) => value === "Ali")
// }
// console.log(check());

const numbers = [2, 5, 8, 12, 19];
let result = numbers.every((value) => value > 0);
console.log(result);